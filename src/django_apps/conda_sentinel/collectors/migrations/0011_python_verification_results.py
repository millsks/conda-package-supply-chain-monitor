# Generated by Django 5.2.15 on 2026-09-09 01:49

# `python_verification_results`, CPM-PY314-S02's verification half of CPM-FR-14.
# Hand-edited once, on the terms 0002 through 0010 were: the autodetector named a
# later `identity` migration, and the only `identity` relation here is the PROTECT
# foreign key to `identity.Package`, which `0001_package_identity` creates.
# Depending on a later identity migration would order this one behind migrations it
# does not need.
#
# PROTECT, as EVIDENCE.02-AUDIT-001 requires of every relation on an evidence model
# -- Django's deletion collector bypasses every append-only refusal in
# core/models.py, so a CASCADE here would destroy a package's verification history
# when the package went.
#
# Otherwise unedited: the model declares the whole of it -- the one read index
# core/freshness.py's query needs, and the three constraints.
#
# **This is the table 0010 is kept apart from.** CPM-FR-14 requires inferred and
# verified compatibility to be distinct recorded states, and two tables is how that
# is made structural: 0010 holds what a project's metadata *claims* and this one
# holds what an execution *did*. The determinate values name proof --
# `verified_compatible` and `verification_failed`, never `ok` and never a bare
# `compatible`, which on a queue beside 0010's `inferred_compatible` would read as
# the same kind of answer (CPM-AD-24). collectors/outcomes.py composes the
# vocabulary and argues why the negative verdict names the *run* rather than
# claiming the package is incompatible.
#
# The first constraint is CPM-PY314-S02's AC 1 as a database rule: a determinate row
# records the platform, the architecture and a log reference, and a row that is not
# determinate records none of the three. One biconditional over three columns rather
# than three rules, because "says where it ran" is one fact -- a row naming a
# platform and no architecture has not said it, and a build succeeds on a platform,
# so a verdict with nowhere attached would be a claim about every platform made from
# an execution on one.
#
# The second is 0010's reason rule reached for the same reason and with a sharper
# edge: `not_applicable` here means this product need never *build* this package,
# the only thing that can establish it is identity's own `not_applicable` mapping,
# and a row carrying the state with no reason would be indistinguishable from one
# written out of an *absence* of identity.
#
# The third requires the verified Python series on every row, sentinel rows
# included, because CPM-PY314-S03 reduces this table and 0010's together.
#
# There is no unique constraint of any kind (CPM-AD-2): verifying a package a second
# time is a new row and the first one stands, which is how a reader sees that a
# package that failed in June builds in September. There is no derived status and no
# readiness verdict: a collector may not compute one (CPM-AD-8) and a policy pass
# writes only its own derived table (CPM-AD-21), so CPM-FR-19's readiness is
# CPM-PY314-S03's. There is no data step: the table starts empty, and -- verification
# being triggered and never swept (AC 3) -- it stays empty for every package nobody
# has asked about, which in a real deployment is most of them.

import django.db.models.deletion
from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('collectors', '0010_python_readiness_assessments'),
        ('identity', '0001_package_identity'),
    ]

    operations = [
        migrations.CreateModel(
            name='PythonVerificationResult',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('observed_at', models.DateTimeField(verbose_name='observed at')),
                ('source', models.CharField(blank=True, default='', max_length=512, verbose_name='source')),
                ('state', models.CharField(choices=[('error', 'Error'), ('unknown', 'Unknown'), ('not_found', 'Not Found'), ('not_applicable', 'Not Applicable'), ('verified_compatible', 'Verified Compatible'), ('verification_failed', 'Verification Failed')], max_length=32, verbose_name='state')),
                ('python_series', models.CharField(max_length=32, verbose_name='python series')),
                ('platform', models.CharField(blank=True, default='', max_length=64, verbose_name='platform')),
                ('architecture', models.CharField(blank=True, default='', max_length=64, verbose_name='architecture')),
                ('log_reference', models.CharField(blank=True, default='', max_length=768, verbose_name='log reference')),
                ('detail', models.TextField(blank=True, default='', verbose_name='detail')),
                ('trace_id', models.CharField(blank=True, default='', max_length=32, verbose_name='trace id')),
                ('package', models.ForeignKey(on_delete=django.db.models.deletion.PROTECT, related_name='python_verification_results', to='identity.package', verbose_name='package')),
            ],
            options={
                'verbose_name': 'python verification result',
                'verbose_name_plural': 'python verification results',
                'db_table': 'python_verification_results',
                'indexes': [models.Index(fields=['package', '-observed_at'], name='py_verify_pkg_observed')],
                'constraints': [models.CheckConstraint(condition=models.Q(models.Q(('state__in', ('verified_compatible', 'verification_failed')), models.Q(('platform', ''), _negated=True), models.Q(('architecture', ''), _negated=True), models.Q(('log_reference', ''), _negated=True)), models.Q(models.Q(('state__in', ('verified_compatible', 'verification_failed')), _negated=True), ('platform', ''), ('architecture', ''), ('log_reference', '')), _connector='OR'), name='verification_says_where_it_ran'), models.CheckConstraint(condition=models.Q(models.Q(('state', 'not_applicable'), _negated=True), models.Q(('detail', ''), _negated=True), _connector='OR'), name='verification_not_applicable_states_its_reason'), models.CheckConstraint(condition=models.Q(('python_series', ''), _negated=True), name='verification_names_the_series_it_ran')],
            },
        ),
    ]
