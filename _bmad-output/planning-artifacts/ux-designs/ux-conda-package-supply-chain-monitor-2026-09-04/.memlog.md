---
topic: CPM-EP-APP surface: current-health view, package detail, three role-scoped queues, six reports, governed API
updated: 2026-09-04T17:50
---

- (decision) UX runs ahead of CPM-EP-APP deliberately: 30 stories remain before APP starts, and epics.md:326 states no UX design contract exists, leaving presentation to the implementer. Closing that gap now.
- (decision) Working mode: coaching path. Creative tools woven in; Kevin picks, Sally does not impose.
- (decision) Stakes: internal. Org-internal tool behind OIDC, three known roles, no public exposure. Reasonable accessibility floor, no formal compliance audit.
- (decision) Sources confirmed by Kevin: epics.md, prd.md, ARCHITECTURE-SPINE.md. solution-design.md and sprint-change proposals excluded as noise for a UX contract.
- (decision) Artifacts land on feature/cpm-ux-app-design cut from main, implemented as a git worktree at ../cpm-ux-app-design because the S04 story file differs from main by 229/93 lines and had uncommitted edits; switching branches in place would have dragged that work onto the wrong base.
- (event) imports/ui-mockups.html added: 2911-line existing mockup from PR #13 'docs(ux): add screen mockups and workflow diagrams for the app surface'. Kevin surfaced it; richest UX input available.
- (assumption) TENSION TO RESOLVE: epics.md:326 asserts no UX design contract exists, but docs/ux/ui-mockups.html has existed since PR #13. Reading: artifacts exist, a binding contract does not. The spines must either codify the mockup decisions or consciously override them, and epics.md:326 needs a follow-up correction either way.
- (event) DEFECT in epics.md: the UX Design Requirements section (L161-169) says 'This is a known gap, not an omission. See the open question raised at the end of this step' -- but no such open question exists. The Open Question table at L151-159 has no UX entry. The gap was flagged and the flag went nowhere. Worth a docs fix alongside the epics.md:326 correction.
- (decision) Hard constraint the spines must encode above all else: the five outcome states render as themselves and never as blank or clean (epics L1403-1405, L1519, L1550-1553, L604). Blank is reserved exclusively for a field with no value (L155). This is the product's core visual problem.
- (assumption) OQ 5 blocks two numbers the UX would normally state: the p95 latency budget and the sync export row cap. The spines must specify the in-progress/async affordance (CPM-APP-S08) without asserting the threshold that triggers it.
- (decision) PRIMARY DESIGN CONSTRAINT, from CPM-SM-C1 (prd.md:746-748): driving the clean-result proportion up by resolving unknown or unmapped into clean is 'the primary failure mode of the entire product'. Any visual treatment that flattens unknown toward clean -- soft grey reading as fine, an empty cell, green-by-default rows -- actively causes the failure the product exists to prevent. This outranks every aesthetic consideration in DESIGN.md.
- (event) Protagonists confirmed from PRD journeys: Dana (security and compliance reviewer, daily, before standup, enters at the queue), Ravi (packaging engineer, enters at the feedstock-gap report, session spans two systems), Sam (platform lead, monthly, enters at the coverage view, only actor who can override package identity). All three they/them. These become the Key Flows protagonists -- no invention needed.
- (assumption) OPEN DECISION for Kevin: queue-to-role mapping is not stated in the PRD and its list order actively misleads. The PRD's own rubric flags this as medium and prescribes a table. Defensible pairing from the permission model: compliance review->security reviewer, remediation->packaging engineer, identity review->platform lead. The UX spec must make this call explicitly and flag it as ours.
- (assumption) PRD specifies ZERO accessibility, browser, device, responsive, dark-mode, density, hierarchy or navigation requirements across 917 lines. The UX contract originates all of it. 'Dashboard' appears exactly once in the PRD -- in the out-of-scope list.
- (assumption) COPY DECISION forced: 'confidence' is overloaded between package-identity confidence (verified/inventory-derived/unmapped) and match confidence (advisory applies to package+version). The PRD glossary forbids abbreviating the latter to 'confidence' but the PRD violates its own rule in FR-11/FR-24/FR-26. Voice and Tone section must pick a side and hold it.
- (event) CONTRADICTION FOUND across all three sources on whether queues are role-exclusive. PRD FR-31: 'queues are role-exclusive'. epics APP.05-API-004: a role opening another role's queue is refused. Spine CPM-AD-13: 'queue write access is role-scoped per transition, NOT per queue, so one item can legitimately be advanced by two roles in sequence.' If the spine wins, the UI computes available actions from the transition table against the item's current state, not from which queue or which role -- a materially different interaction model. Must be resolved before CPM-APP-S05 is written.
- (event) IA CONSTRAINT from NAVIGATION_REGISTRY (allowlist.py:324-345): nav entries are data not markup -- a label string, a URL name, an optional permission for filtering. No icons, no submenus, and NO COUNT BADGES ('Remediation (14)' is impossible). Order is inherited from adopted_apps order and cannot be specified. No side nav, tab bar or breadcrumb exists in the platform. Everything richer is in-page navigation designed from scratch inside the content area.
- (event) STACK GROUND TRUTH: server-rendered Django templates on Bootstrap 5.2.3 (cdnjs+SRI) with crispy-forms, plus DRF + drf-spectacular. No HTMX, no JS framework, no build step anywhere in spine or repo. The spine states only the DRF half; the template half is inherited convention, not an invariant. Any client-side interaction technique is a NEW decision the spine neither authorizes nor forbids -- and therefore does not protect. Needs Kevin's call.
- (decision) MANDATORY PROVENANCE CHROME: CPM-AD-11 states computed_at, the run cut-off and the per-domain version map are displayed by EVERY view and export. This is a UX requirement written as an architecture invariant -- provenance is not a progressive-disclosure detail, it is required chrome on every list, every detail page, every export.
- (decision) NO OPTIMISTIC UI: CPM-AD-23 requires every privileged write and its audit row to be one atomic unit in one service function -- never on_commit, never a follow-up task. The UI must not report success before the audit lands.
- (event) The mockup is far more than a sketch: a complete token system (30+ CSS custom properties), implemented dark mode dual-gated on prefers-color-scheme and data-theme, a shape-coded status chip vocabulary, five hand-authored inline SVG diagrams built on the same tokens, prefers-reduced-motion honoured, and a self-declared invented-vs-settled ledger at lines 2829-2872. Discovery shifts from eliciting a visual identity to codifying and reconciling one.
- (decision) The mockup already solves the CPM-SM-C1 problem and the solution is excellent: the four sentinel states are given hues deliberately OFF the green-amber-red determinate ramp (unknown=slate, not_found=blue, not_applicable=violet, error=burnt orange) AND their own marker shapes (hollow ring, dashed ring, struck circle, triangle) AND the lowercase status word as visible text AND an evidence footline. Four redundant channels. A sentinel cannot be misread as a severity position. DESIGN.md should codify this as-is.
- (assumption) LARGEST OPEN QUESTION for DESIGN.md: the mockup masthead claims 'Bootstrap 5.2, inherited from the platform base' but the file is drawn in an entirely bespoke IBM Plex + teal token system. The closing note admits the tension and promises every component 'maps onto what the platform already loads' but supplies no mapping table. Bootstrap badge/table/modal defaults look nothing like .chip/.tbl/.modal. Needs Kevin's call: is the token system the contract, or is Bootstrap?
- (assumption) chip.warn is overloaded three ways -- determinate amber (lagging, medium, manual review), the stale overlay, and remediation blocked. The no-collapse doctrine holds for the four sentinels but breaks down on warn. Also the chip label vocabulary is unbounded: 60+ label strings, some prose fragments, some carrying data. EXPERIENCE.md needs a rule separating status VALUES (closed set, lowercase, verbatim) from chip LABELS (currently open).
- (event) The mockup declares 'Django templates - HTMX - Alpine.js' in its masthead and documents a precise HTMX boundary (four interactions earn it: facet swaps, evidence disclosure, queue transitions, job polling; everything else a plain page load; Alpine holds view state only and never computes a status, count or timestamp). The ARCHITECTURE-SPINE never mentions HTMX or Alpine anywhere. So the mockup made a stack decision the spine does not record, authorize or protect.
- (decision) Mockup a11y gaps to close in EXPERIENCE.md: no h1 inside any app screen (starts at h2, jumps to h4); no aria-live on the async toast; no aria-sort on the sorted column; the filterpill remove control is a made-up <x> element with no accessible name; modals lack role=dialog/aria-modal/aria-labelledby/focus trap; errmsg not tied via aria-describedby; select-all checkbox unlabelled; a.locked is a link not a disabled button. Good bones already present: semantic elements, wrapped facet labels, global focus-visible ring, prefers-reduced-motion, SVG role=img with descriptive labels.
- (decision) STACK (Kevin, explicit): the UI is Python-native by intent -- Django templates, crispy-forms for form rendering, HTMX for interaction, Alpine.js for view state. Design tokens are plain CSS custom properties in static/css/project.css. NO build step, no npm, no JS framework. Proof it needs no JS: ui-mockups.html renders the entire token system in 2911 lines with zero script tags. Bootstrap 5.2 stays for grid, utilities and crispy-forms output; tokens own semantic colour and every semantic component (.chip, .ev, .freshbar, .tbl) overrides Bootstrap defaults.
- (decision) PYTHON-SIDE ENFORCEMENT of the no-collapse rule: a status chip is ONE Django inclusion tag, e.g. {% status_chip pkg.vulnerability_status evidence=pkg.vuln_evidence %}, never markup copy-pasted across templates, so reporting renders every chip through a single code path. Same for {% freshbar %} -- the mockup already claims it is 'a single inclusion tag ... so a surface cannot forget it'. EXPERIENCE.md specifies both.
- (decision) HTMX/Alpine boundary codified in EXPERIENCE.md, with a flag that the ARCHITECTURE-SPINE should adopt it as an invariant since it currently neither authorizes nor protects it. The boundary as drawn: HTMX earns four interactions -- facet swaps on the health view, evidence disclosure on package detail, queue transitions posting a form and swapping a row, job polling. Everything else is a plain page load. Alpine holds view state only (open panels, pending filter selections, bulk checkboxes, modal focus) and never computes a status, a count or a timestamp. Every partial is a template fragment the full page also renders.
- (decision) QUEUE ACCESS RESOLVED (Kevin): the PRD and the spine were never in conflict -- they govern different layers. Opening a queue page is role-exclusive (403 and a logged refusal otherwise), satisfying PRD CPM-FR-31 and epics APP.05-API-004. Acting on an item draws available actions from the (from_state, to_state, required_role) transition table against the item's current state, satisfying spine CPM-AD-13. An item routed between queues is the SAME object carrying its history; two roles advance it in sequence, each from their own queue page. No source document needs correcting. EXPERIENCE.md states both halves explicitly so nobody conflates them.
- (decision) STALE RESOLVED (Kevin): stale is a PROPERTY OF a status, not a status in its own right. The chip keeps the underlying state's hue and gains a stale marker plus the amber .ev line -- 'critical + stale' stays crit-hued, 'clean + stale' stays ok-hued. This frees --warn to mean exactly one thing (determinate amber: lagging, medium, manual review) and moves 'blocked' onto the CPM-FR-41 readiness axis (ready/blocked/unknown/stale) rather than the outcome-status axis. Corrects the mockup, which rendered stale as an amber chip and overloaded --warn three ways.
- (change) Post-distill fix: EXPERIENCE.md carried 8 malformed token references ({type.mono}, {focus.ring}, {layout.split}) and cited four components DESIGN.md never declared (facet, filterpill, pager, field) plus the split layout. Added all five to DESIGN.md frontmatter and body, corrected the three malformed reference forms. Verified: 75 refs, 0 unresolved, both frontmatters valid YAML.
- (event) G-2, the gap that could not be closed by design: CPM-UJ-2's climax is Ravi filing a tracking issue and the tracking URL being recorded. FR-27 limits v1 writes to the package-identity override and queue actions, and CPM-AD-10 gives the application no third write path -- so the PRD's own journey has a climax the PRD's own requirements prohibit. EXPERIENCE.md writes the beat as the PRD states it, flags it inline, and gives the only version the current contract permits: draft composed read-only, tracked state carried as a workflow item advanced to 'already tracked'. Resolving it needs either a third write verb in the PRD plus a spine invariant, or acceptance of the degraded flow. That is a product decision, not a design one.
- (event) reconcile-ui-mockups.md written: 21 mockup decisions adopted wholesale, 14 corrected (with reasons), 7 deferred to named gaps, 4 dropped, plus 5 qualitative ideas preserved that no spine section fully captures. Standing rule recorded: spines win on conflict; the mockup stays in imports/ unmodified as the historical record.
- (decision) G-2 RESOLVED (Kevin): v1 ships draft composition as a read-only artifact; tracked state rides on a workflow item advanced to 'already tracked'. No third write path, FR-27 intact, tracking URL not stored in v1. Reopening requires a PRD amendment AND a spine invariant, in that order.
- (decision) G-6 RESOLVED (Kevin): desktop-only floor, declared in DESIGN.md as a decision rather than an omission. No narrow layout designed; the mockup's gestured-at offcanvas is not assumed. The WCAG AA 1.4.10 Reflow shortfall is recorded in the Accessibility Floor so adopting this spine is not mistaken for meeting AA in full.
- (change) Rubric walker returned 5 high / 10 medium / 11 low. Both section orders exact, all eight CPM-APP stories addressed, every source invariant carried, stale-as-a-property applied with zero residue, withheld numbers held. Fixed all five highs: H1 eight dotted token refs ({components.chip.plain} etc) corrected to the hyphenated keys -- these were the three components carrying the no-collapse doctrine, and my own earlier validator passed them because it split on the first dot; H2 the feedstock-gap report registered as G-18 (Ravi's entry surface is not one of the six reports, same defect as G-9 which was flagged while this one was not); H3 three sibling no-collapse invariants added (CPM-FR-17 KEV never averaged into severity, CPM-FR-10 channels never merged, CPM-FR-16 currency per surface); H4 the health view's eleven-column set stated; H5 viewport minimum fixed at one figure, 1180px, stated once in DESIGN and referenced from EXPERIENCE.
- (decision) Validator lesson worth keeping: a token-reference checker must resolve the FULL dotted path against the frontmatter tree, not just the first segment. Splitting on the first dot passes {components.chip.plain} because 'chip' exists, while the actual lookup returns null. Verified fix: 75 refs, 0 unresolved under full-path resolution, with nested typography paths like {typography.mono-chip.fontSize} resolving correctly.
- (change) Accessibility lens returned 2 critical / 7 high / 12 medium / 6 low / 27 passes. All criticals and the structural highs fixed. C-1: the job toast polled hx-swap=outerHTML every 3s inside an aria-live region containing the Cancel button -- keyboard users were reset to body every 3s and could never reach Cancel, screen-reader users heard the whole toast every 3s. Split into a static shell (job id, trace id, Cancel) that never swaps and an inner status node that polls with aria-atomic=false, announcing only on a real phase change. C-2: focus after a row swap was specified two mutually impossible ways on the primary write path (the trigger is inside the row being replaced). One rule now: a swap containing focus moves focus to the swapped-in replacement carrying tabindex=-1; a swap not containing focus never moves it; never body.
- (change) H-4, the most consequential finding of the whole session: staleness did not survive the export. Its three carriers -- a CSS ::after mark, an amber footline, a visually-hidden span -- none exist in a CSV, and rule L2 deliberately keeps 'stale' out of the chip label. So a stale clean would have exported as clean: CPM-SM-C1 exactly, in the one artifact that leaves the system, while the document asserted stale never displays as clean. This defect was introduced by my own stale-as-a-property design, not inherited from the mockup. Fixed: every status column in an export now requires two companion columns, <domain>_observed_at and <domain>_stale, asserted by the column-set diff test CPM-AD-24 already mandates.
- (change) Contrast: every light-mode failure traced to one token. --muted #63767e failed against accent-wash (3.96), paper (4.03), surface-3 (4.10) and surface-2 (4.41) -- grounds carrying every evidence footline in the product. Changed to #556670, which clears all four (4.98/5.06/5.15/5.54) with hue and ladder position preserved; verified independently. Dark mode passed everywhere (5.15-9.12). Recorded as no-touch: --unk on --unk-wash is the tightest passing pair at 4.537 with almost no headroom, and it is the one state that must never be easy to overlook. Superseded rows lose opacity:.62 (3.45 light / 4.48 dark) and are marked by their chip and placement instead.
- (change) Structural root the lens identified: EXPERIENCE.md delegated 'contrast ratios and target sizes' to DESIGN.md, which never accepted either -- it stated no ratio requirement and never used the phrase 'target size'. Every measurable failure lived in that seam, including WCAG 2.5.8 target-size failures on the filter-pill remove control (~12x12px, the very control whose semantics EXPERIENCE.md had already fixed) and the evidence disclosure caret. DESIGN.md now carries a 'Contrast and target size -- accepted here, not delegated' section. Same seam produced H-5: DESIGN specified the visually-hidden stale span and delegated markup to EXPERIENCE, whose Accessibility Floor never mentioned stale, so the sole accessible carrier of staleness would not have been built. Also fixed H-1 (no skip link or landmarks, a Level A 2.4.1 gap inside an AA floor, acute with 30+ facets ahead of the table) and M-7 (Reflow cited as 200% zoom when 1.4.10 is a 320px/400% criterion, understating the gap).
- (change) Three mocks rendered and promoted to mockups/: empty-and-error-states (E1-E5 plus 404, 500, session expiry), stale-overlay (the hue proof, the old-vs-new comparison, a greyscale plate, a theme toggle marked as a specimen affordance), accept-risk-form (empty, invalid, committed, with the audit-row contract). Caught on promotion: all three carried the pre-fix --muted #63767e because they rendered while the contrast fix was being applied. Corrected in place; all 27 light tokens now match DESIGN.md exactly, and zero hex values sit outside the :root blocks in any of the three.
- (change) Three corrections the mock render surfaced, applied back to EXPERIENCE.md: (1) HTTP-code chips (403/404/500/session-expiry) must use the markerless .chip-plain variant -- the mockup drew 403 with .chip.crit, putting a non-outcome fact on the determinate ramp inside the vocabulary reserved for the five states. (2) 'clean' is prose, not an OutcomeState member -- there is no clean enum value, each domain has its own determinate good value (none found, allowed, present), and the PRD's own rubric flags that clean is never defined. Legitimate in prose, a heading or a counter-metric label; never a chip label, enum value or column heading. (3) The page-size cap needed a source: it is the DRF pagination class's max_page_size, and the HTML pager reads the same two constants the API does so a view and its API equivalent cannot disagree about what a page is.
- (assumption) Deliberate absence recorded on the accept-risk mock: no expiry or 'review again on' field. No source defines one, and CPM-APP-S04's whole point is that an accepted finding stays accepted across re-observation. Stated on the page with its reasoning so it can be argued with rather than silently assumed.
- (change) Polish pass (structure + prose lenses per doc_standards): ~50 prose edits, 18 headings renamed to name the rule rather than the topic, 5 duplicated rule statements consolidated to one authoritative site each, bare 'identity' fixed in 5 places and bare 'confidence' in 7. Structural: DESIGN.md gained an H1 (it had started at H2, a skipped level), Colors resequenced so the token block is followed by what each colour is for rather than by the contrast section, target size moved out of Colors into Layout & Spacing where a control-internal metric belongs, and the health-view column set moved from Interaction Primitives into Information Architecture -- it had been pasted mid-way through a bullet list, splitting it in two.
- (change) Six content problems the polish pass surfaced and declined to adjudicate, all fixed: (1) .field body specified 11.5px/12px literals the type ramp had already retired -- snapped to ramp tokens; (2) two class names for one component, .pager vs .pagination/.pagenums -- reconciled; (3) the split grid disagreed three ways, and minmax(0,1fr) is the form that matters because a bare 1fr has an automatic min-content minimum, so an 11-column table would push the grid wider than its container instead of scrolling inside it; (4) nobody had stated where the 1180px floor actually breaks -- computed: roughly 110% zoom on a 1280px display, and the floor is 3.7x wider than the 320px Reflow asks for; (6) the Empty row routed to E2-E5 but E5 is explicitly not an empty state -- corrected to E2-E4; (7) the stated tracking range topped out at +0.13em when nothing in the ramp exceeds +0.1em. Left alone as reasoned: the -ize cluster in 'authorization', which is internally consistent and pairs with Django's AUTHENTICATION_BACKENDS.
- (event) spines finalized
